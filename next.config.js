/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.jennexplores.com", "placeimg.com", "upload.wikimedia.org", 'cloudflare-ipfs.com', 'avatars.githubusercontent.com'],
  },
}

module.exports = nextConfig
